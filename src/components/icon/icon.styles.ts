import { css } from 'lit';

export default css`
  :host {
    display: inline-block;
    stroke-width: 1.5px;
  }

  svg {
    display: block;
    width: auto;
    height: 1em;
    stroke-width: inherit;
  }
`;
