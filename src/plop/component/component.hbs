import { customElement, property } from 'lit/decorators.js';
import { html } from 'lit';
import { QuietElement } from '../../utilities/quiet-element.js';
import hostStyles from '../../styles/host.styles.js';
import styles from './{{ removePrefix tag }}.styles.js';
import type { CSSResultGroup } from 'lit';

/**
 * <{{ tag }}>
 *
 * @summary What does this component do?
 * @documentation https://quietui.com/docs/components/{{ removePrefix tag }}
 * @status stable
 * @since ___
 *
 * @cssproperty [--foo=bar] - Description of foo.
 */
@customElement('{{ tag }}')
export class {{ tagToClassName tag }} extends QuietElement {
  static styles: CSSResultGroup = [hostStyles, styles];

  /** The property description. */
  @property() example = 'TODO';

  render() {
    return html` <slot></slot> `;
  }
}
