import { css } from 'lit';

export default css`
  :host {
    display: inline-flex;
    background-color: var(--quiet-neutral-deep);
    font-size: 0.75em;
    font-weight: var(--quiet-font-weight-semibold);
    border-radius: 9999px;
    color: var(--quiet-text-deep);
    padding: 0.1em 0.7em;
  }
`;
